import theme, { COLORS, SIZES, FONTS } from "./theme";
import config from './firebaseFiles';
import images from './images'

export { 
    theme, 
    COLORS, 
    SIZES, 
    FONTS, 
    config, 
    images, 
};
